.DS_Store
node_modules/
*.log

# Dated snapshots kept on disk while reworking a page. This repo publishes with
# `git add -A` (anything untracked is a 404 waiting to happen), so an
# unignored backup would ship a superseded copy of the site to the live host.
*.bak-*
